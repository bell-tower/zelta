output_dir: tmp
shellspec_name: "050_zelta_tests_spec"
describe_desc: "Run more zelta commands on divergent tree"
test_list:
  - test_name: revert_after_rotate
    it_desc: revert after rotate - %{when_command}
    when_command: zelta revert "$SANDBOX_ZELTA_SRC_EP" "$SANDBOX_ZELTA_TGT_EP"
